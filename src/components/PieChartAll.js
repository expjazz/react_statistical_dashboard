import React from 'react';

export default function PieChartAll() {
  // grab the data

  // treat the data
  // pass to chart
  // do event listeners to change the data
  return (
    <div>
      Hello from Pie
    </div>
  );
}
